% doubleturan.sty
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{doubleturan}[2025/06/02 v1.1 Double Turán Problem style]

% Encoding & margins
\RequirePackage[utf8]{inputenc}
\RequirePackage[left=1in,top=1in,right=1in,bottom=1in]{geometry}

% Bibliography & cross‐refs
\RequirePackage[numbers]{natbib}
\RequirePackage[linktocpage=true]{hyperref}

% Math & theorem environments
\RequirePackage{amsmath,amssymb,amsthm,thmtools}
\RequirePackage{mathtools}

% Graphics & colors
\RequirePackage{graphicx}
\RequirePackage{xcolor}
\RequirePackage{tikz}

% Algorithms
\RequirePackage{algorithm}
\RequirePackage{algpseudocode}

% Listings, enumeration, titles, spacing
\RequirePackage{listings}
\RequirePackage{enumitem}
\RequirePackage{titlesec}
\RequirePackage{setspace}

% Miscellaneous
\RequirePackage{comment}

% ----------------------------------------
% Custom counter for “Conjecture” and “OldTheorem”
\newcounter{foo}

% Theorem‐style declarations
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{claim}{Claim}

% “Conjecture” uses counter foo, printed as a letter (A, B, …)
\newtheorem{conjecture}{Conjecture}[foo]
\renewcommand\theconjecture{\Alph{conjecture}}

\newtheorem{definition}{Definition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}

% “OldTheorem” also shares counter foo and prints as a letter
\newtheorem{oldtheorem}[foo]{Theorem}
\renewcommand\theoldtheorem{\Alph{oldtheorem}}

\setlength{\marginparwidth}{2cm}

\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}
\renewcommand{\baselinestretch}{1.2}

% ----------------------------------------
% Custom macros
\newcommand*{\Q}{\mathbb{Q}}
\newcommand*{\R}{\mathbb{R}}
\newcommand*{\C}{\mathbb{C}}
\newcommand*{\N}{\mathbb{N}}
\newcommand*{\Z}{\mathbb{Z}}
\newcommand*{\prob}{\mathds{P}}
\newcommand*{\E}{\mathds{E}}
\newcommand*{\F}{\mathcal{F}}
\newcommand*{\ex}{\textnormal{ex}}
\newcommand*{\con}{\mathcal{C}}

\newcommand{\Var}{\mathrm{Var}} 
\newcommand{\Cov}{\mathrm{Cov}} 
